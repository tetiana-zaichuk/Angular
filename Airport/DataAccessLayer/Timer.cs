using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;
using System.Timers;
using DataAccessLayer.Models;

namespace BusinessLayer
{
    class Timer
    {/*
        private static async void OnTimedEventForDelay(Object source, ElapsedEventArgs e)
        {
            await Flightss;
        }

        public Task<Flight> UseTimer()
        {
            var tcs = new TaskCompletionSource<Flight>();
            System.Timers.Timer _delayTimer = new System.Timers.Timer(3000);
            _delayTimer.Elapsed += OnTimedEventForDelay;
            _delayTimer.AutoReset = false;
            _delayTimer.Enabled = true;

            return tcs.Task;
        }*/
    }
}
